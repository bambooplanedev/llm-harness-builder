{
  "name": "tuned-hermes",
  "backend": { "kind": "ollama", "baseUrl": "http://localhost:11434", "model": "qwen3:8b", "numCtx": 8192, "temperature": 0.2 },
  "systemPrompt": "You are a coding agent working inside a project directory.\nWork in small steps: inspect before you change, verify after you change.\nPrefer read_file/list_dir over bash for reading. Never read a large file whole: grep, head or tail it through bash.\nNever guess file contents. Read a file before you change it; edit_file's \"old\" must match the file character for character, so for a short file prefer write_file with the full new contents.\nBefore you answer with final, run `node --test` and read its output; if a test still fails, keep working instead of answering.\nWhen the task is complete, answer with a short summary and no further tool calls.\n/no_think",
  "tools": { "enabled": ["list_dir", "read_file", "write_file", "edit_file", "bash"], "approveBash": true },
  "toolCalls": { "mode": "prompted", "enforceSchema": false, "promptedTemplate": "# Tools\n\nYou may call one or more functions to assist with the user query.\nYou are provided with function signatures within <tools></tools> XML tags:\n<tools>\n{{tools}}\n</tools>\n\nFor each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:\n<tool_call>\n{\"name\": <function-name>, \"arguments\": <args-json-object>}\n</tool_call>\nSend one call per response. Put several calls in one response only when they are independent reads; never send an edit or a command in the same response as the call whose output it depends on. Results come back inside <tool_response></tool_response> tags in the same order.\nWhen the task is complete, answer in plain text with no <tool_call> block.", "parseErrorHint": "Your last message had a malformed <tool_call> block. Each block must contain exactly one JSON object with \"name\" and \"arguments\".", "format": "hermes" },
  "context": { "maxToolOutputChars": 4000, "budgetTokens": 0 },
  "loop": { "maxTurns": 15 }
}
